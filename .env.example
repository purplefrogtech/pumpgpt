# ============================================
# PUMP•GPT v2.x - Example Configuration
# ============================================

# TELEGRAM BOT
BOT_TOKEN=your_bot_token_here
TELEGRAM_CHAT_IDS=123456789,987654321
VIP_USER_IDS=123456789
DEBUG_LEVEL=INFO            # INFO / DEBUG / TRACE

# BINANCE API (for real trading, leave empty for demo)
BINANCE_API_KEY=your_api_key_here
BINANCE_API_SECRET=your_api_secret_here

# SYMBOLS TO SCAN
SYMBOLS=BTCUSDT,ETHUSDT,SOLUSDT,BNBUSDT,XRPUSDT,ADAUSDT,AVAXUSDT,MATICUSDT,LINKUSDT,OPUSDT,ARBUSDT

# SCANNER SETTINGS
TIMEFRAME=15m               # Allowed: 15m,30m,1h
HTF_TIMEFRAME=1h            # Higher timeframe for confirmation (fixed to swing bias)
SCAN_INTERVAL_SECONDS=60    # How often to scan (min 30s)
SCAN_CONCURRENCY=3          # How many symbols processed in parallel
COOLDOWN_MINUTES=5          # Minimum time between signals for same symbol (internal)
THROTTLE_MINUTES=30         # Minutes between same-symbol signals (external gate)

# STRATEGY PARAMETERS
STRATEGY_NAME=PUMP-GPT v2.1-Optimized
DEFAULT_LEVERAGE=10
VOLUME_SPIKE_RATIO=1.5

# QUALITY FILTER (RELAXED to avoid zero signals)
MIN_RISK_REWARD=1.2
MIN_RSI=30
MAX_RSI=70
MIN_ATR_PCT=0.00015         # 0.015%
MIN_VOLUME_RATIO=1.05
MAX_SPREAD_PCT=0.01         # 1%
MIN_SUCCESS_RATE=25

# THROTTLE (Spam Prevention)
# Persisted in signal_throttle.json

# SIMULATOR / RISK MANAGEMENT
SIM_EQUITY_USD=10000        # Starting capital for simulation
SIM_RISK_PER_TRADE_PCT=1.0  # Risk per trade (%)
SIM_TP1_RATIO_QTY=0.5       # Close 50% at TP1
SIM_BE_ON_TP1=1             # Move SL to breakeven after TP1
SIM_FEE_BPS=8               # Exchange fee (0.08%)
SIM_NOTIFY=1                # Notify on trade events

# TEST SIGNAL COMMAND
TEST_SIGNAL_SYMBOL=BTCUSDT
TEST_SIGNAL_TIMEFRAME=15m
